extends layout

block content
  //if (!user)
  a(href="/login") Login
  //  br
    a(href="/register") Register
  //if (user)
  p You are currently logged in
    //as #{user.username}
  a(href="/logout") Logout

  if (data)
    p the data is: #{data.toString()}

  if (consentUsers)
   p I have access to the following users #{consentUsers.toString()}
